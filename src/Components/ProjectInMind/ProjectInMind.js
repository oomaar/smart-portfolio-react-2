import {
    ProjectInMindContainer,
} from "./styledProjectInMind";

export const ProjectInMind = () => {
    return (
        <ProjectInMindContainer>
            ProjectInMind
        </ProjectInMindContainer>
    );
};
